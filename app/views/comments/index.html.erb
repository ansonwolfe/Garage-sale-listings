<fieldset>
	
<legend>Comments for products</legend>

<% @comments.each do |comment| %>
	<!-- show the product the comments are associated with -->
	<strong>Product name: <a href="/products/<%= comment.product.id %>"><%= comment.product.name %></a></strong>
	<small>ID #: <%= comment.product.id %></small>
	<blockquote>
	<%= comment.content %><br>
	<small>
	<%= comment.created_at %>
	</small>
	</blockquote>
		
<% end %>	
</fieldset>